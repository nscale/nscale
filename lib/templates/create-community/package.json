{
  "name": "{{name}}",
  "version": "{{version}}",
  "main": "{{name}}.js",
  "scripts": {
    "start": "usrv-dev",
    "isolated": "usrv-dev --iso"
  },
  "dependencies": {
    "usrv": "^0.2.4"
  },
  "devDependencies": {
    "usrv-dev": "^0.1.1"
  }
}
