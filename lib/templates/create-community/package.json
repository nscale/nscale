{
  "name": "{{name}}",
  "version": "{{version}}",
  "main": "{{name}}.js",
  "scripts": {
    "start": "usrv-dev",
    "isolated": "usrv-dev --iso"
  },
  "dependencies": {
    "usrv": "^0.2.7"
  },
  "devDependencies": {
    "usrv-dev": "^0.3.3"
  }
}
